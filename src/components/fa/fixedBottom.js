import React, { Component } from 'react';
import {
    View,
} from 'react-native';

export default class FixedBottom extends Component {
    static propTypes = {

    };
    static defaultProps = {

    };
    render() {
        return <View/>
    }
}
